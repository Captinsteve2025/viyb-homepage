# Environment variables for VIYB Next.js app
#
# Usage:
# - For LOCAL development, copy this file to `.env.local` (or `.env`) and fill in values.
# - For NETLIFY deployment, add these variables in Site settings → Build & deploy → Environment.
#   Netlify does not automatically import `.env` files from the repo.
#
# General
NEXT_DISABLE_TELEMETRY=1

# SMTP configuration for enquiry API (src/app/api/enquiry/route.ts)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
ENQUIRY_TO_EMAIL=info@virginislandsyachtbroker.com

# Optional external data sources for yacht listings (src/lib/api/yachts.ts)
# Provide only one, or leave all empty to use mock data.
YACHTWORLD_API_KEY=
WORDPRESS_URL=
CUSTOM_YACHT_FEED_URL=
